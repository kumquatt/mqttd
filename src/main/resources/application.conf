echoserver {
  #take defaults from common configuration
  include "common"

  akka {
    remote.netty.tcp {
      #hostname = "127.0.0.1"
      #port = 8888
    }
  }
}


mqtt {
  #take defaults from common configuration
  include "common"
  loglevel = DEBUG

  akka {
    remote.netty.tcp {
    }
  }
}

